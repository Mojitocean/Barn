<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.barn.order.infra.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="com.barn.order.domain.entity.Order">
        <id column="id" property="id"/>
        <result column="order_sn" property="orderSn"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status" typeHandler="org.apache.ibatis.type.EnumOrdinalTypeHandler"/>
        <!-- 级联查询明细 (生产环境建议分开查) -->
        <collection property="items" ofType="com.barn.order.domain.entity.OrderItem"
                    select="com.barn.order.infra.mapper.OrderItemMapper.selectByOrderId" column="id"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO oms_order (order_sn, user_id, total_amount, pay_amount, status, create_time)
        VALUES (#{orderSn}, #{userId}, #{totalAmount}, #{payAmount}, #{status.code}, #{createTime})
    </insert>

    <select id="selectBySn" resultMap="OrderResultMap">
        SELECT * FROM oms_order WHERE order_sn = #{orderSn}
    </select>
</mapper>