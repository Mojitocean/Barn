<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.barn.user.infra.mapper.AddressMapper">
    <select id="selectById" resultType="com.barn.user.domain.entity.Address">
        SELECT id, user_id, name, phone_number, default_status, province, city, region, detail_address
        FROM ums_address
        WHERE id = #{id}
    </select>
</mapper>