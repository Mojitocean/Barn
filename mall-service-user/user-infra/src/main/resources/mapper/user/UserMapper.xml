<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.barn.user.infra.mapper.UserMapper">
    <select id="selectById" resultType="com.barn.user.domain.entity.User">
        SELECT id, username, nickname, mobile, email, avatar, status, member_level_id, create_time, login_time
        FROM ums_user
        WHERE id = #{id}
    </select>
</mapper>